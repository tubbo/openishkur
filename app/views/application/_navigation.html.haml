%nav.top-bar{ data: { topbar: true }, role: 'navigation' }
  %ul.title-area
    %li.name
      %h1= link_to config.title, root_path
  -# %section#search.top-bar-section
  -#   %ul
  -#     %li.has-dropdown
  -#       = form_tag url: search_path, method: :get, remote: true do
  -#         = search_field_tag :query, nil, placeholder: 'Search...', autocomplete: 'off'
  -#       %ul#results.dropdown
  -#         / Search Results
  %section#users.top-bar-section
    %ul.right
      - if logged_in?
        %li= link_to current_user.name, current_user
        %li= link_to t(:sign_out, scope: 'devise.links'), destroy_user_registration_path, method: :delete
      - else
        %li= link_to_modal t(:sign_in, scope: 'devise.links'), new_user_session_path
        %li= link_to_modal t(:sign_up, scope: 'devise.links'), new_user_registration_path

